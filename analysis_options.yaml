include: package:lints/recommended.yaml

analyzer:
  exclude:
    - "**/build/**"
    - "**/.dart_tool/**"

linter:
  rules:
    avoid_print: true
    directives_ordering: true
    prefer_final_locals: true
