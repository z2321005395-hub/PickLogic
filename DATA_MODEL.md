# Data Model

The canonical model and interface contract is maintained at [`docs/DATA_MODEL.md`](docs/DATA_MODEL.md).

Compatibility rule: update that document and add an ADR before changing shared contracts used by more than one target.
