name: picklogic_system_insight_core
description: Read-only storage and system explanation primitives for PickLogic Pro.
version: 0.1.0-alpha
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  picklogic_core_models:
    path: ../core_models

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
