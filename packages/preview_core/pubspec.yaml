name: picklogic_preview_core
description: Bounded preview and thumbnail cache primitives for PickLogic.
version: 0.1.0-alpha
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
