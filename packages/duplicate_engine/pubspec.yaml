name: picklogic_duplicate_engine
description: Streaming SHA-256 and exact duplicate grouping for PickLogic.
version: 0.1.0-alpha
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  crypto: ^3.0.6
  picklogic_core_models:
    path: ../core_models

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
