name: picklogic_literature_core
description: Local literature metadata and safe naming primitives for PickLogic Pro.
version: 0.1.0-alpha
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  petit_bibtex: 6.2.0
  picklogic_core_models:
    path: ../core_models
  sqlite3: 3.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
