name: picklogic_test_fixtures
description: Synthetic-only fixtures for PickLogic tests and demos.
version: 0.1.0-alpha
publish_to: none

environment:
  sdk: ^3.12.0

resolution: workspace

dependencies:
  picklogic_core_models:
    path: ../packages/core_models

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
