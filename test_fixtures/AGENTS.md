# Synthetic Fixture Rules

- Everything here must be generated or intentionally synthetic and safe for public Git.
- Never copy real filenames, paper titles, screenshots, metadata, paths, or device content.
- Keep fixtures tiny; generate large-count/performance fixtures locally and ignore them.
- A manifest documents expected categories, duplicate groups, protections, and OCR state.
