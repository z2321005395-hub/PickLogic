# PickLogic Desktop

One Flutter desktop app with Standard (`lib/main_standard.dart`) and Pro (`lib/main_pro.dart`) entrypoints. See the repository root documentation and this directory's `AGENTS.md`.
